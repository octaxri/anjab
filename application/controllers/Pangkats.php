<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Pangkats extends CI_Controller
{
    function __construct()
    {
        parent::__construct();

        $this->load->model('Pangkats_model');
        $this->load->library('form_validation');

        if(!$this->session->userdata('logined') || $this->session->userdata('logined') != true)
        {
            redirect('/');
        }        
	$this->load->library('datatables');
    }

    public function index()
    {
        $this->load->view('pangkats/pangkats_list');
    } 
    
    public function json() {
        header('Content-Type: application/json');
        echo $this->Pangkats_model->json();
    }

    public function read($id) 
    {
        $row = $this->Pangkats_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'nama_pangkat' => $row->nama_pangkat,
	    );
            $this->load->view('pangkats/pangkats_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('pangkats'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('pangkats/create_action'),
	    'id' => set_value('id'),
	    'nama_pangkat' => set_value('nama_pangkat'),
	);
        $this->load->view('pangkats/pangkats_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'nama_pangkat' => $this->input->post('nama_pangkat',TRUE),
	    );

            $this->Pangkats_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('pangkats'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Pangkats_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('pangkats/update_action'),
		'id' => set_value('id', $row->id),
		'nama_pangkat' => set_value('nama_pangkat', $row->nama_pangkat),
	    );
            $this->load->view('pangkats/pangkats_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('pangkats'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'nama_pangkat' => $this->input->post('nama_pangkat',TRUE),
	    );

            $this->Pangkats_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('pangkats'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Pangkats_model->get_by_id($id);

        if ($row) {
            $this->Pangkats_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('pangkats'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('pangkats'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('nama_pangkat', 'nama pangkat', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Pangkats.php */
/* Location: ./application/controllers/Pangkats.php */
/* Please DO NOT modify this information : */
/* Generated by mahrus@pcr.ac.id*/
