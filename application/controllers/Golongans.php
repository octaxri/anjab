<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Golongans extends CI_Controller
{
    function __construct()
    {
        parent::__construct();

        $this->load->model('Golongans_model');
        $this->load->library('form_validation');

        if(!$this->session->userdata('logined') || $this->session->userdata('logined') != true)
        {
            redirect('/');
        }        
	$this->load->library('datatables');
    }

    public function index()
    {
        $this->load->view('golongans/golongans_list');
    } 
    
    public function json() {
        header('Content-Type: application/json');
        echo $this->Golongans_model->json();
    }

    public function read($id) 
    {
        $row = $this->Golongans_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'nama_gol' => $row->nama_gol,
	    );
            $this->load->view('golongans/golongans_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('golongans'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('golongans/create_action'),
	    'id' => set_value('id'),
	    'nama_gol' => set_value('nama_gol'),
	);
        $this->load->view('golongans/golongans_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'nama_gol' => $this->input->post('nama_gol',TRUE),
	    );

            $this->Golongans_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('golongans'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Golongans_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('golongans/update_action'),
		'id' => set_value('id', $row->id),
		'nama_gol' => set_value('nama_gol', $row->nama_gol),
	    );
            $this->load->view('golongans/golongans_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('golongans'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'nama_gol' => $this->input->post('nama_gol',TRUE),
	    );

            $this->Golongans_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('golongans'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Golongans_model->get_by_id($id);

        if ($row) {
            $this->Golongans_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('golongans'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('golongans'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('nama_gol', 'nama gol', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Golongans.php */
/* Location: ./application/controllers/Golongans.php */
/* Please DO NOT modify this information : */
/* Generated by mahrus@pcr.ac.id*/
