<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Jabatan_fungsional_angka_kredit extends CI_Controller
{
    function __construct()
    {
        parent::__construct();

        $this->load->model('Jabatan_fungsional_angka_kredit_model');
        $this->load->library('form_validation');

        if(!$this->session->userdata('logined') || $this->session->userdata('logined') != true)
        {
            redirect('/');
        }        
	$this->load->library('datatables');
    }

    public function index()
    {
        $this->load->view('jabatan_fungsional_angka_kredit/jabatan_fungsional_angka_kredit_list');
    } 
    
    public function json() {
        header('Content-Type: application/json');
        echo $this->Jabatan_fungsional_angka_kredit_model->json();
    }

    public function read($id) 
    {
        $row = $this->Jabatan_fungsional_angka_kredit_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'angka' => $row->angka,
	    );
            $this->load->view('jabatan_fungsional_angka_kredit/jabatan_fungsional_angka_kredit_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('jabatan_fungsional_angka_kredit'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('jabatan_fungsional_angka_kredit/create_action'),
	    'id' => set_value('id'),
	    'angka' => set_value('angka'),
	);
        $this->load->view('jabatan_fungsional_angka_kredit/jabatan_fungsional_angka_kredit_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'angka' => $this->input->post('angka',TRUE),
	    );

            $this->Jabatan_fungsional_angka_kredit_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('jabatan_fungsional_angka_kredit'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Jabatan_fungsional_angka_kredit_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('jabatan_fungsional_angka_kredit/update_action'),
		'id' => set_value('id', $row->id),
		'angka' => set_value('angka', $row->angka),
	    );
            $this->load->view('jabatan_fungsional_angka_kredit/jabatan_fungsional_angka_kredit_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('jabatan_fungsional_angka_kredit'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'angka' => $this->input->post('angka',TRUE),
	    );

            $this->Jabatan_fungsional_angka_kredit_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('jabatan_fungsional_angka_kredit'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Jabatan_fungsional_angka_kredit_model->get_by_id($id);

        if ($row) {
            $this->Jabatan_fungsional_angka_kredit_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('jabatan_fungsional_angka_kredit'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('jabatan_fungsional_angka_kredit'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('angka', 'angka', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Jabatan_fungsional_angka_kredit.php */
/* Location: ./application/controllers/Jabatan_fungsional_angka_kredit.php */
/* Please DO NOT modify this information : */
/* Generated by mahrus@pcr.ac.id*/
