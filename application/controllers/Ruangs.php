<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Ruangs extends CI_Controller
{
    function __construct()
    {
        parent::__construct();

        $this->load->model('Ruangs_model');
        $this->load->library('form_validation');

        if(!$this->session->userdata('logined') || $this->session->userdata('logined') != true)
        {
            redirect('/');
        }        
	$this->load->library('datatables');
    }

    public function index()
    {
        $this->load->view('ruangs/ruangs_list');
    } 
    
    public function json() {
        header('Content-Type: application/json');
        echo $this->Ruangs_model->json();
    }

    public function read($id) 
    {
        $row = $this->Ruangs_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'nama_ruang' => $row->nama_ruang,
	    );
            $this->load->view('ruangs/ruangs_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('ruangs'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('ruangs/create_action'),
	    'id' => set_value('id'),
	    'nama_ruang' => set_value('nama_ruang'),
	);
        $this->load->view('ruangs/ruangs_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'nama_ruang' => $this->input->post('nama_ruang',TRUE),
	    );

            $this->Ruangs_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('ruangs'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Ruangs_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('ruangs/update_action'),
		'id' => set_value('id', $row->id),
		'nama_ruang' => set_value('nama_ruang', $row->nama_ruang),
	    );
            $this->load->view('ruangs/ruangs_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('ruangs'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'nama_ruang' => $this->input->post('nama_ruang',TRUE),
	    );

            $this->Ruangs_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('ruangs'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Ruangs_model->get_by_id($id);

        if ($row) {
            $this->Ruangs_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('ruangs'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('ruangs'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('nama_ruang', 'nama ruang', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Ruangs.php */
/* Location: ./application/controllers/Ruangs.php */
/* Please DO NOT modify this information : */
/* Generated by mahrus@pcr.ac.id*/
